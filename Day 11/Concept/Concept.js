/*
   Note=> 
    1) Promise.all => This method takes an iterable of promises (such as an array) and returns a single promise that resolves when all the promises in the iterable have resolved, or rejects if any of the promises reject

   2) Promise.race => take multiple promise and return any one promise that executed as soon as possible

*/ 